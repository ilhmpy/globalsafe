import * as Styled from "./Notify.styles";
import { FC } from "react";

type NotifyProps = {
   block: boolean; 
};

export const Notify: FC<NotifyProps> = ({ block }: NotifyProps) => {
    return (
        <>

        </>
    )
};